# CLI Tools

## aiterm

Terminal optimizer for AI-assisted development with Claude Code and Gemini CLI.

```bash
brew install data-wise/tap/aiterm
ait doctor       # Check installation
ait detect       # Show project context
ait switch       # Apply context to terminal
```

**Type:** Python virtualenv | **Source:** GitHub

## atlas

ADHD-friendly project state engine with sessions, captures, and context.

```bash
brew install data-wise/tap/atlas
```

**Type:** Node npm | **Source:** GitHub

## examark

Create exams from Markdown and export to Canvas QTI format.

```bash
brew install data-wise/tap/examark
```

**Type:** Node npm | **Source:** npm registry

## flow-cli

ZSH workflow tools designed for ADHD brains.

```bash
brew install data-wise/tap/flow-cli
g status         # Git status
g feature start  # Start feature branch
tm switch        # Context switching
```

**Type:** Shell (ZSH) | **Source:** GitHub

## mcp-bridge

Chrome extension + SSE server for connecting Claude.ai to local MCP servers.

```bash
brew install data-wise/tap/mcp-bridge
brew services start mcp-bridge
```

**Type:** Node npm | **Source:** GitHub

## nexus-cli

Knowledge workflow CLI for research, teaching, and writing.

```bash
brew install data-wise/tap/nexus-cli
```

**Type:** Python virtualenv | **Source:** PyPI

## scribe-cli

Scribe document conversion CLI.

```bash
brew install data-wise/tap/scribe-cli
```

**Type:** Swift | **Source:** GitHub
